{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "prepare-cli",
            "type": "shell",
            "command": "npx prisma generate && npm run build",
            "problemMatcher": ["$tsc"]
        }
    ]
}